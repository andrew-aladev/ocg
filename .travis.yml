language: ruby
rvm:
  - 2.7

dist: focal
osx_image: xcode11.6

jobs:
  include:
    - os: linux
      arch: amd64
    - os: osx
      arch: amd64

before_install:
  - gem install bundler

script:
  - scripts/ci_test.sh
